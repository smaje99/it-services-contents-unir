---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<section class:list={['card-content', className]} {...rest}>
  <slot />
</section>

<style>
  .card-content {
  }
</style>
